# QA Lab Brasil 3.1.2 - Simulator UX Local

Scope limited to simulator runner UX; 3.1.1 content/PDFs remain unchanged.

- Desktop/notebook runner navigation compacted and capped to simulator content width.
- Mobile runner navigation remains fixed but is raised above the current Netlify badge area.
- Mobile question/answer spacing compacted so A/B/C/D fit in one viewport whenever content length allows.
- Mobile runner now focuses the question card rather than the large progress header after each Next/Previous action.
- Back-to-top is hidden only while a mobile practice is active; chatbot is moved above the runner to avoid overlap.
- Runner UI is cleaned when navigating away from /simulators and when returning to the simulator selection screen.
- PT/EN/ES labels continue to use the existing simulator localization.
- Powered by Netlify is intentionally NOT changed in this release.
